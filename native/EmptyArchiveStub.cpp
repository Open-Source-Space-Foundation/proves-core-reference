// Intentionally (nearly) empty. macOS ar refuses to create archives with zero
// members; this stub is added to library targets whose every source has been
// moved elsewhere by F Prime CONFIGURATION_OVERRIDES (see native/CMakeLists.txt).
namespace {
const int proves_native_empty_archive_stub = 0;
}
