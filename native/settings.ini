; Native (host) build entry for F Prime Component UTs and SIL deployments.
; Run fprime-util from this directory:
;   cd native && fprime-util generate --ut && fprime-util build --ut --all
; then: ctest --test-dir build-fprime-automatic-native-ut -R PROVESFlightControllerReference_
; fprime-zephyr is safe to include here: its module registration is guarded on
; FPRIME_PLATFORM=Zephyr (OSSF fork, Open-Source-Space-Foundation/fprime-zephyr#28),
; so no Zephyr-only Os/Drv modules register in native builds.
[fprime]
project_root: ..
framework_path: ../lib/fprime
library_locations: ../lib/fprime-zephyr:../lib/fprime-extras
default_cmake_options: FPRIME_ENABLE_FRAMEWORK_UTS=OFF
                       FPRIME_ENABLE_AUTOCODER_UTS=OFF
                       FPRIME_CMAKE_QUIET=ON
