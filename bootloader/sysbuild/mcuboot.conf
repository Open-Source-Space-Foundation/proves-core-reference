# Sysbuild per-image Kconfig fragment (sysbuild/<image>.conf) for MCUboot.
# MCUboot shares the board defconfig but has no CONFIG_LOG, so usp_zephyr does
# not link. Disabling LORA_BASICS_MODEM_DRIVERS also disables USP.
CONFIG_LORA_BASICS_MODEM_DRIVERS=n
